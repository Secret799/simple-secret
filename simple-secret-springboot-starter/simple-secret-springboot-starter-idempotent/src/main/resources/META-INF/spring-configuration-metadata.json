{
  "groups": [
    {
      "name": "simple-secret.idempotent",
      "type": "com.ss.idempotent.config.IdempotentProperties",
      "sourceType": "com.ss.idempotent.config.IdempotentProperties"
    }
  ],
  "properties": [
    {
      "name": "simple-secret.idempotent.enabled",
      "type": "java.lang.Boolean",
      "defaultValue": true,
      "description": "是否启用 Servlet 重复提交保护；启用时必须存在 IdempotencyStore 或 RedissonClient。"
    },
    {
      "name": "simple-secret.idempotent.key-prefix",
      "type": "java.lang.String",
      "defaultValue": "simple-secret:idempotent:",
      "description": "分布式幂等租约的 key 前缀。"
    },
    {
      "name": "simple-secret.idempotent.identity-header",
      "type": "java.lang.String",
      "defaultValue": "Authorization",
      "description": "默认身份解析器优先读取的可信请求头。"
    }
  ]
}
